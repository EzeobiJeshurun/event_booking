import React, {Fragment, memo } from 'react';
import PropTypes from 'prop-types';
 
const Schedule = () => {
    return (
        <Fragment>
          <div>
            Bookings
          </div>
        </Fragment>
    );
}
 
Schedule.propTypes = {};
 
export default memo(Schedule);